## HPA

Contains Kubernetes HPA configuration and a script for enabling or disabling autoscaling.

### hpa.yaml
Kubernetes Horizontal Pod Autoscaler configuration.

### hpa_controller.sh
Script for enabling or disabling HPA in the cluster.

Usage:
- ./hpa_controller.sh enable
- ./hpa_controller.sh disable